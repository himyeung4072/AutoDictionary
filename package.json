{
  "name": "dictation-app-tests",
  "version": "1.0.0",
  "description": "Tests for 默書神器",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "fast-check": "^3.0.0",
    "jsdom": "^23.0.0"
  }
}
